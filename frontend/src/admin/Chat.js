import React from 'react';

const Chat = () => {
    // Logic của component ở đây

    return (
        <div>
            {/* JSX của component ở đây */}
        </div>
    );
};

export default Chat;